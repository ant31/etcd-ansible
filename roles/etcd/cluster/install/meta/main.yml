---
dependencies:
  - name: etcd

  - name: etcd/cluster/facts

  - name: adduser
    vars:
      user: "{{ etcd_user }}"
    when:
      - not (ansible_os_family in ['CoreOS', 'Container Linux by CoreOS'])

  - name: etcd/certs/fetch
    tags:
      - etcd-certs

# @TODO(ant31) not in is_atomic
